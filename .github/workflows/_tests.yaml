name: 📝 Tests

on: workflow_call

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 📥 Install
        uses: ./.github/actions/package-install

      - name: Run tests
        run: npm test
