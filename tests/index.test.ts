import * as vCard from "../lib/index";

// partially generated with chatGPT

describe("index.ts", () => {
	it("should export Property", () => { expect(vCard.PropertyIO).toBeDefined(); });

	it("should export VCard", () => { expect(vCard.VCardIO).toBeDefined(); });

	it("should export ICaseMap", () => { expect(vCard.ICaseMap).toBeDefined(); });
});
